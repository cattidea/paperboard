{
  "devDependencies": {
    "vitepress": "^0.4.0"
  },
  "scripts": {
    "docs:dev": "vitepress dev",
    "docs:build": "vitepress build",
    "docs:test": "vitepress build && npx serve .vitepress/dist"
  }
}
